<section class="callout-links">
  <h2 class="callout-links__title">{{title}}</h2>

  <ul class="callout-links__list">
    {% for item in items %}
      <li class="callout-links__item">
        <a href="{{item.url}}" class="callout-links__link link-border">{{item.text}}</a>
      </li>
    {% endfor %}
  </ul>
</section>
