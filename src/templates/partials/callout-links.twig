<section class="callout-links">
  <h1 class="callout-links__title">{{title}}</h1>

  <ul class="callout-links__list">
    {% for item in items %}
      <li class="callout-links__item">
        <a href="{{item.url}}" class="callout-links__link">{{item.text}}</a>
      </li>
    {% endfor %}
  </ul>
</section>
