<div class="course-list">

  <p class="course-list__title">Courses offered in the past four years.</p>

  {% set current_icon = '<span class="course-list__icon" title="Offered in the current term">●</span>' %}
  {% set upcoming_icon = '<span class="course-list__icon" title="Offered in the upcoming term">○</span>' %}

  <p class="course-list__meta">
    {{current_icon}} <em>indicates offered in the current term</em><br>
    {{upcoming_icon}} <em>indicates offered in the upcoming term(s)</em>
  </p>

  <div class="course-list__items">

    {% for i in 0..5 %}

      {% set id = loop.index %}

      {# data-toggle-group should be a unique value if there are multiple on the page #}
      {# js-course-<ID> can be the course/id value #}
      <div class="course-item js-course-{{id}}">
        <h4 class="course-item__title" id="course-label-{{id}}">
          <a
            href="#course-{{id}}"
            data-toggle-target=".js-course-{{id}}"
            data-toggle-group="courselist"
            aria-expanded="false"
            aria-controls="course-{{id}}">
            TRLM 8535 - Website Localization
          </a>
          {# "randomly" render some example combinations of icons #}
          {% if loop.first %}
            {{current_icon}}
          {% endif %}
          {% if loop.index == 3 %}
            {{current_icon}}
            {{upcoming_icon}}
          {% endif %}
          {% if loop.index == 2 %}
            {{upcoming_icon}}
          {% endif %}
        </h4>
        <div class="course-item__content" id="course-{{id}}" aria-labelledby="#course-label-{{id}}">
          <p class="course-item__text">
            The purpose of this course is to familiarize students with web technologies, development and localization. We will look at
            this topic from the point of view of the project manager, the localization engineer, and even the translator. The
            hands-on style of this course will put students in situations that require critical thinking, risk mitigation skills
            and solid knowledge of the web localization industry—all of which will be honed throughout this class. Specifically,
            we will be covering the following topics: creating and localizing websites using opensource frameworks such as WordPress
            and Drupal, web technologies such as HTML, CSS and responsive design and JavaScript, wiki technology such as MediaWiki
            and Dokuwiki, bug tracking in Jira and The Bug Genie, and proxy localization such as Transifex and Smartling.
          </p>
          <div class="course-item__requirements"></div>
          <div class="course-item__terms">Fall 2017 - MIIS</div>
          <a href="http://catalog.middlebury.edu/courses/view/catalog/catalog%2FMIIS/course/course%2FTRLM8535" class="course-item__link link-border">View in Course Catalog</a>
        </div>
      </div>
    {% endfor %}

  </div>

</div>
