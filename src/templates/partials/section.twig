<section class="section">
  <header class="section__header">
    <h1 class="section__title">{{title}}</h1>
    <div class="section__nav">
      <a href="{{moreLinkHref}}" class="section__nav-link">{{moreLinkText}}</a>
    </div>
  </header>
  {% block content %}{% endblock %}
</section>
